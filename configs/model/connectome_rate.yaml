model:
  kind: connectome_rate
  encoder: cnn
  leak: 0.5
  weight_scale: 0.05
  training_mode: trainable_internal
  normalize_incoming: true
  sign_constraints: false
  activation: tanh
  propagation_steps: 5
  baseline_hidden_size: 128
